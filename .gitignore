# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / frontend
node_modules/
frontend/dist/

# NOTE: src/static/ (the built React app) IS committed on purpose, so the repo
# is deploy-ready without Node — an in-workspace agent (e.g. Genie Code) or a
# plain `bundle deploy` can ship it as-is. Rebuild with `npm run build` after
# changing the frontend (see AGENTS.md).

# Databricks
.databricks/
*.env
.env

# OS / editor
.DS_Store
.idea/
.vscode/

# coverage artifacts
.coverage
coverage.xml
htmlcov/
